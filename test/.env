export OMP_NUM_THREADS = 8

export MATRIX_CDIM = $(shell echo "2^5" | bc)
export MATRIX_RDIM = $(shell echo "2^5" | bc)

export ADD_RECURSIVE_THRESHOLD = $(shell echo "2^2" | bc)
export MUL_RECURSIVE_THRESHOLD = $(shell echo "2^2" | bc)
